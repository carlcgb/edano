name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Check Google Maps API key
        run: |
          if [ -z "${{ secrets.VITE_GOOGLE_MAPS_API_KEY }}" ]; then
            echo "❌ VITE_GOOGLE_MAPS_API_KEY is not set in GitHub Secrets"
            exit 1
          fi
          echo "✅ VITE_GOOGLE_MAPS_API_KEY is configured from GitHub Secrets"
          API_KEY_PREVIEW="${{ secrets.VITE_GOOGLE_MAPS_API_KEY }}"
          echo "🔑 API Key preview: ${API_KEY_PREVIEW:0:15}..." 

      - name: Setup environment file
        run: |
          echo "🔍 Verifying API key is available..."
          if [ -z "$VITE_GOOGLE_MAPS_API_KEY" ]; then
            echo "❌ ERROR: VITE_GOOGLE_MAPS_API_KEY is not available"
            echo "❌ This means the secret is not set in GitHub Secrets"
            exit 1
          fi
          echo "✅ API key is available (length: ${#VITE_GOOGLE_MAPS_API_KEY} chars)"
          echo "🔑 API key preview: ${VITE_GOOGLE_MAPS_API_KEY:0:20}..."
          echo "📝 Creating .env.production file..."
          echo "VITE_GOOGLE_MAPS_API_KEY=$VITE_GOOGLE_MAPS_API_KEY" > .env.production
          echo "✅ .env.production created"
          cat .env.production | sed 's/VITE_GOOGLE_MAPS_API_KEY=\(.\{20\}\).*/VITE_GOOGLE_MAPS_API_KEY=\1.../' 
        env:
          VITE_GOOGLE_MAPS_API_KEY: ${{ secrets.VITE_GOOGLE_MAPS_API_KEY }}

      - name: Build
        run: |
          echo "📦 Starting build with VITE_GOOGLE_MAPS_API_KEY..."
          echo "🔑 API key length: ${#VITE_GOOGLE_MAPS_API_KEY} chars"
          echo "🔑 API key preview: ${VITE_GOOGLE_MAPS_API_KEY:0:20}..."
          if [ -z "$VITE_GOOGLE_MAPS_API_KEY" ]; then
            echo "❌ CRITICAL ERROR: VITE_GOOGLE_MAPS_API_KEY is empty!"
            echo "❌ Build will fail - secret must be set in GitHub Secrets"
            exit 1
          fi
          npm run build
          echo "✅ Build completed"
          echo "🔍 Verifying API key was injected in built files..."
          if find dist/assets -name "*.js" -type f -exec grep -q "AIzaSy" {} \; 2>/dev/null; then
            echo "✅ SUCCESS: API key found in built JavaScript files"
          else
            echo "❌ CRITICAL ERROR: API key NOT found in built files!"
            echo "Checking for undefined values..."
            if find dist/assets -name "*.js" -type f -exec grep -q '"undefined"' {} \; 2>/dev/null || find dist/assets -name "*.js" -type f -exec grep -q "undefined" {} \; 2>/dev/null; then
              echo "❌ Found 'undefined' - API key was not injected during build"
              echo "This is a critical error - build will fail"
              exit 1
            else
              echo "⚠️ WARNING: Could not verify API key injection - but continuing"
            fi
          fi
        env:
          VITE_GOOGLE_MAPS_API_KEY: ${{ secrets.VITE_GOOGLE_MAPS_API_KEY }}

      - name: Check Cloudflare secrets
        run: |
          if [ -z "${{ secrets.CLOUDFLARE_API_TOKEN }}" ]; then
            echo "❌ CLOUDFLARE_API_TOKEN is not set"
            exit 1
          fi
          if [ -z "${{ secrets.CLOUDFLARE_ACCOUNT_ID }}" ]; then
            echo "❌ CLOUDFLARE_ACCOUNT_ID is not set"
            exit 1
          fi
          echo "✅ Cloudflare secrets are configured"

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: 'edano-podcast'
          directory: './dist'
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}